[
  {
    "id": "sel_001",
    "dimension": "selection",
    "prompt": "What's the weather like in San Francisco today?",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "get_weather",
          "description": "Get the current weather for a city",
          "parameters": {
            "type": "object",
            "properties": {
              "city": { "type": "string", "description": "City name" },
              "units": { "type": "string", "enum": ["celsius", "fahrenheit"], "description": "Temperature units" }
            },
            "required": ["city"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "get_news",
          "description": "Get the latest news headlines",
          "parameters": {
            "type": "object",
            "properties": {
              "topic": { "type": "string", "description": "News topic or keyword" },
              "limit": { "type": "number", "description": "Number of articles to return" }
            },
            "required": ["topic"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "get_weather", "args": { "city": "San Francisco" }, "argsMatchMode": "subset" }]
    },
    "difficulty": "easy"
  },
  {
    "id": "sel_002",
    "dimension": "selection",
    "prompt": "Search the web for the latest news about AI startups",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "search_web",
          "description": "Search the internet for information",
          "parameters": {
            "type": "object",
            "properties": {
              "query": { "type": "string", "description": "Search query" }
            },
            "required": ["query"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "get_weather",
          "description": "Get the current weather for a city",
          "parameters": {
            "type": "object",
            "properties": {
              "city": { "type": "string" }
            },
            "required": ["city"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "send_email",
          "description": "Send an email to a recipient",
          "parameters": {
            "type": "object",
            "properties": {
              "to": { "type": "string" },
              "subject": { "type": "string" },
              "body": { "type": "string" }
            },
            "required": ["to", "subject", "body"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "search_web", "args": { "query": "AI startups" }, "argsMatchMode": "subset" }]
    },
    "difficulty": "easy"
  },
  {
    "id": "sel_003",
    "dimension": "selection",
    "prompt": "Add 2 + 2 for me",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "calculator",
          "description": "Perform mathematical calculations",
          "parameters": {
            "type": "object",
            "properties": {
              "expression": { "type": "string", "description": "Mathematical expression to evaluate" }
            },
            "required": ["expression"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "query_database",
          "description": "Execute a SQL query against the database",
          "parameters": {
            "type": "object",
            "properties": {
              "sql": { "type": "string" }
            },
            "required": ["sql"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "calculator", "args": { "expression": "2 + 2" }, "argsMatchMode": "subset" }]
    },
    "difficulty": "easy"
  },
  {
    "id": "sel_004",
    "dimension": "selection",
    "prompt": "Schedule a meeting with John at 3pm tomorrow",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "create_calendar_event",
          "description": "Create a new event on the calendar",
          "parameters": {
            "type": "object",
            "properties": {
              "title": { "type": "string" },
              "attendees": { "type": "array", "items": { "type": "string" } },
              "start_time": { "type": "string", "description": "ISO 8601 datetime" },
              "duration_minutes": { "type": "number" }
            },
            "required": ["title", "start_time"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "send_slack_message",
          "description": "Send a message via Slack",
          "parameters": {
            "type": "object",
            "properties": {
              "channel": { "type": "string" },
              "message": { "type": "string" }
            },
            "required": ["channel", "message"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "set_reminder",
          "description": "Set a personal reminder",
          "parameters": {
            "type": "object",
            "properties": {
              "text": { "type": "string" },
              "time": { "type": "string" }
            },
            "required": ["text", "time"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "create_calendar_event", "args": {}, "argsMatchMode": "subset" }]
    },
    "difficulty": "medium"
  },
  {
    "id": "sel_005",
    "dimension": "selection",
    "prompt": "Look up the stock price of Apple",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "get_stock_price",
          "description": "Get the current stock price for a ticker symbol",
          "parameters": {
            "type": "object",
            "properties": {
              "ticker": { "type": "string", "description": "Stock ticker symbol, e.g. AAPL" }
            },
            "required": ["ticker"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "get_company_info",
          "description": "Get general company information and fundamentals",
          "parameters": {
            "type": "object",
            "properties": {
              "company_name": { "type": "string" }
            },
            "required": ["company_name"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "get_market_news",
          "description": "Get financial market news and analysis",
          "parameters": {
            "type": "object",
            "properties": {
              "topic": { "type": "string" }
            },
            "required": ["topic"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "get_stock_price", "args": { "ticker": "AAPL" }, "argsMatchMode": "subset" }]
    },
    "difficulty": "easy"
  },
  {
    "id": "sel_006",
    "dimension": "selection",
    "prompt": "Translate 'Hello, how are you?' to Spanish",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "translate_text",
          "description": "Translate text from one language to another",
          "parameters": {
            "type": "object",
            "properties": {
              "text": { "type": "string" },
              "target_language": { "type": "string", "description": "Target language code (ISO 639-1)" },
              "source_language": { "type": "string", "description": "Source language code (optional)" }
            },
            "required": ["text", "target_language"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "detect_language",
          "description": "Detect the language of a text",
          "parameters": {
            "type": "object",
            "properties": {
              "text": { "type": "string" }
            },
            "required": ["text"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "translate_text", "args": { "text": "Hello, how are you?", "target_language": "es" }, "argsMatchMode": "subset" }]
    },
    "difficulty": "medium"
  },
  {
    "id": "sel_007",
    "dimension": "selection",
    "prompt": "Get me the list of files in the /tmp directory",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "list_files",
          "description": "List files and directories in a given path",
          "parameters": {
            "type": "object",
            "properties": {
              "path": { "type": "string", "description": "Directory path" },
              "recursive": { "type": "boolean", "description": "Whether to list recursively" }
            },
            "required": ["path"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "read_file",
          "description": "Read the contents of a file",
          "parameters": {
            "type": "object",
            "properties": {
              "path": { "type": "string" }
            },
            "required": ["path"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "delete_file",
          "description": "Delete a file from the filesystem",
          "parameters": {
            "type": "object",
            "properties": {
              "path": { "type": "string" }
            },
            "required": ["path"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "list_files", "args": { "path": "/tmp" }, "argsMatchMode": "subset" }]
    },
    "difficulty": "easy"
  },
  {
    "id": "sel_008",
    "dimension": "selection",
    "prompt": "Send an email to alice@example.com saying the report is ready",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "send_email",
          "description": "Send an email to a recipient",
          "parameters": {
            "type": "object",
            "properties": {
              "to": { "type": "string" },
              "subject": { "type": "string" },
              "body": { "type": "string" }
            },
            "required": ["to", "subject", "body"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "send_sms",
          "description": "Send a text message to a phone number",
          "parameters": {
            "type": "object",
            "properties": {
              "phone_number": { "type": "string" },
              "message": { "type": "string" }
            },
            "required": ["phone_number", "message"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "send_email", "args": { "to": "alice@example.com" }, "argsMatchMode": "subset" }]
    },
    "difficulty": "easy"
  },
  {
    "id": "sel_009",
    "dimension": "selection",
    "prompt": "Execute this Python code: print('hello world')",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "run_code",
          "description": "Execute code in a sandboxed environment",
          "parameters": {
            "type": "object",
            "properties": {
              "language": { "type": "string", "enum": ["python", "javascript", "bash"] },
              "code": { "type": "string" }
            },
            "required": ["language", "code"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "write_file",
          "description": "Write content to a file",
          "parameters": {
            "type": "object",
            "properties": {
              "path": { "type": "string" },
              "content": { "type": "string" }
            },
            "required": ["path", "content"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "run_code", "args": { "language": "python" }, "argsMatchMode": "subset" }]
    },
    "difficulty": "easy"
  },
  {
    "id": "sel_010",
    "dimension": "selection",
    "prompt": "Find all users in the database who signed up last month",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "query_database",
          "description": "Execute a SQL query against the database",
          "parameters": {
            "type": "object",
            "properties": {
              "sql": { "type": "string", "description": "SQL query to execute" },
              "params": { "type": "array", "items": { "type": "string" } }
            },
            "required": ["sql"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "search_users",
          "description": "Search users by name or email in the user directory",
          "parameters": {
            "type": "object",
            "properties": {
              "query": { "type": "string" },
              "limit": { "type": "number" }
            },
            "required": ["query"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "export_report",
          "description": "Export a business report in CSV format",
          "parameters": {
            "type": "object",
            "properties": {
              "report_type": { "type": "string" },
              "date_range": { "type": "string" }
            },
            "required": ["report_type"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "query_database", "args": {}, "argsMatchMode": "subset" }]
    },
    "difficulty": "medium"
  },
  {
    "id": "sel_011",
    "dimension": "selection",
    "prompt": "What's the current Bitcoin price in USD?",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "get_crypto_price",
          "description": "Get current cryptocurrency price",
          "parameters": {
            "type": "object",
            "properties": {
              "symbol": { "type": "string", "description": "Crypto symbol, e.g. BTC" },
              "currency": { "type": "string", "description": "Fiat currency to price in" }
            },
            "required": ["symbol"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "get_stock_price",
          "description": "Get current stock price by ticker symbol",
          "parameters": {
            "type": "object",
            "properties": {
              "ticker": { "type": "string" }
            },
            "required": ["ticker"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "get_forex_rate",
          "description": "Get foreign exchange rate between two currencies",
          "parameters": {
            "type": "object",
            "properties": {
              "from_currency": { "type": "string" },
              "to_currency": { "type": "string" }
            },
            "required": ["from_currency", "to_currency"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "get_crypto_price", "args": { "symbol": "BTC" }, "argsMatchMode": "subset" }]
    },
    "difficulty": "medium"
  },
  {
    "id": "sel_012",
    "dimension": "selection",
    "prompt": "Get the git log for the last 5 commits",
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "run_git_command",
          "description": "Execute a git command in the repository",
          "parameters": {
            "type": "object",
            "properties": {
              "command": { "type": "string", "description": "Git subcommand and arguments" },
              "repo_path": { "type": "string" }
            },
            "required": ["command"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "read_file",
          "description": "Read the contents of a file",
          "parameters": {
            "type": "object",
            "properties": {
              "path": { "type": "string" }
            },
            "required": ["path"]
          }
        }
      }
    ],
    "expected": {
      "calls": [{ "name": "run_git_command", "args": { "command": "log" }, "argsMatchMode": "subset" }]
    },
    "difficulty": "easy"
  }
]
